paths:
  /songs/keys/getInfo.json:
    post:
      summary: Get Key Info
      tags:
        - Songs
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                id: {type: string}
                files: {type: boolean}
      responses:
        '200':
          description: 'Success'
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/ArrangementKeyResponse'
                  - $ref: '#/components/schemas/ErrorResponse'
                discriminator:
                  propertyName: status
                  mapping:
                    ok: '#/components/schemas/ArrangementKeyResponse'
                    fail: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ArrangementKeyResponse:
      allOf:
        - $ref: '#/components/schemas/Response'
        - type: object
          properties:
            key:
              type: array
              items: {$ref: '#/components/schemas/ArrangementKey'}
