components:
  schemas:
    EventModifiedResponse:
      allOf:
        - $ref: '#/components/schemas/Response'
        - type: object
          properties:
            event:
              type: object
              properties:
                id: {type: string}
    EventStatus: {type: string, enum: [public, private, draft]}